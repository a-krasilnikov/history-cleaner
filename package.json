{
  "name": "history-auto-cleaner",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that auto-deletes chosen sites from browser history.",
  "scripts": {
    "test": "node --test",
    "pack": "git archive --format=zip -o history-auto-cleaner.zip HEAD -- manifest.json background.js options.html options.css options.js icons"
  }
}
